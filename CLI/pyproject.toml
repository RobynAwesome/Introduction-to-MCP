[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "mcp-cli"
version = "0.1.0"
dependencies = [
    "python-dotenv",
    "mcp",
    "pydantic",
    "pyinstaller",
    "anthropic"
]

[project.scripts]
mcp-cli = "main:cli_entry"