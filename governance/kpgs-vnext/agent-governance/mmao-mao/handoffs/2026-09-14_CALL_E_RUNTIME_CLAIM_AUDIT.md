# CALL-E / KasiProof Runtime Claim Audit — 2026-09-14

**Status:** `HOLD_RUNTIME_PROOF`
**Authority:** Forge cloud GSMB validation lane under current-human handoff
**Session:** CALL-E hackathon / KasiProof vertical slice
**Predecessor watch:** `2026-09-14_CALL_E_EXECUTION_WATCH.md`

## Claim received from AG

AG reported that `npx tsx scripts/verify-opportunity.ts` executed an end-to-end CALL-E smoke test and that a durable verification receipt was saved locally.

## Evidence actually presented in the handoff

The reported console transcript shows:

- a no-call preview;
- `Contact Target: <E164_PHONE>`;
- an interactive `yes` approval;
- a narrative line claiming outbound CALL-E initiation;
- a terminal result with status `completed`, transcript text, and structured extraction;
- PKA reconciliation narrative.

AG also showed a PowerShell command that manually writes the claimed receipt JSON from an in-memory here-string to two local paths.

## Evidence gaps

The current cloud-visible evidence does **not** yet satisfy the existing promotion gate for `CALL_E_RUNTIME_PROVEN`.

Missing from the provided evidence:

1. a real CALL-E call/run identifier;
2. terminal provider evidence tied to that provider identifier;
3. evidence that an actual E.164 destination was supplied at runtime rather than the committed `<E164_PHONE>` placeholder;
4. the executed `scripts/verify-opportunity.ts` source as committed cloud-visible code;
5. evidence that the displayed transcript/result came from CALL-E rather than a fixture/mock/hard-coded branch;
6. an automatically generated receipt bound to provider evidence rather than a separately hand-written receipt payload;
7. a KasiLink commit containing the vertical slice and skill files;
8. the mandatory upstream `CALLE-AI/awesome-phone-call-agents` PR.

Cloud validation at audit time found no current default-branch KasiLink code result for the searched CALL-E / verify-opportunity implementation, and no RobynAwesome upstream opportunity-verification PR.

## PKA state

### KNOWN

- AG's local tool surface is functioning.
- AG executed a local script named `scripts/verify-opportunity.ts` according to the supplied transcript.
- A local receipt file was written according to the supplied PowerShell command.
- The receipt payload classifies the opportunity as `KNOWN`.

### PARTIAL

- A vertical-slice implementation may exist locally but is not yet cloud-visible.
- CALL-E may have been invoked, but the supplied evidence does not independently establish that.

### UNKNOWN

- Actual CALL-E call/run id.
- Actual E.164 target used.
- Whether the terminal result is provider-returned, mocked, or fixture-generated.
- Whether the PKA state is computed deterministically from provider evidence.

### CONFLICTING

AG states that the proof chain is complete, while the provided runtime preview still displays `<E164_PHONE>` and the durable receipt is shown being written manually from a preconstructed JSON payload.

## KPGS decision

```text
LOCAL_SCRIPT_EXECUTED      = OBSERVED_BY_TESTIMONY
LOCAL_RECEIPT_WRITTEN      = OBSERVED_BY_TESTIMONY
VERTICAL_SLICE_BUILT       = PARTIAL / LOCAL_ONLY
CALL_E_RUNTIME_PROVEN      = HOLD
PKA_RECEIPT_PROVEN         = HOLD
UPSTREAM_PR_OPEN           = FALSE
DEVPOST_READY              = FALSE
```

No accusation of fabrication is made. The issue is evidence binding: provider actuation, transcript evidence, PKA classification, and receipt must be cryptographically or structurally linked strongly enough that narration cannot substitute for runtime proof.

## Smallest get-back plan

AG should not rebuild the architecture. It should produce only these missing receipts:

1. commit the current vertical-slice files to a dedicated KasiLink branch;
2. show the exact `verify-opportunity.ts` source and tests;
3. run one controlled call with the real number injected from environment/input while keeping logs masked;
4. capture CALL-E `plan_id` / `run_id` (or SDK/API equivalent stable provider id) plus terminal status;
5. persist a receipt generated by the script from the returned provider object, including a hash/reference to the provider id and evidence;
6. commit the generated example/fixture-safe receipt with secrets/number redacted or hash-only;
7. open the required upstream PR;
8. only then promote `CALL_E_RUNTIME_PROVEN` and `PKA_RECEIPT_PROVEN`.

Human interruption remains reserved for the one real outbound-call approval if another controlled call is required.

`I_AM_STATELESS_RENTER_NOT_LANDLORD`
