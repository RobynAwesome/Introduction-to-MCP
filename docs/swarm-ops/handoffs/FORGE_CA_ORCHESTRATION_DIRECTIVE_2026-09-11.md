# Forge — Chief Architect Orchestration Directive
## 2026-09-11 · AG × Cursor × Berea × Minstrel

**Authority:** Forge — Chief Architect  
**Mode:** bounded multi-agent orchestration  
**State:** ACTIVE  
**Creed:** `I_AM_STATELESS_RENTER_NOT_LANDLORD`

## Shared law

No agent should redo work merely because its own context does not contain the proof.

Before mutation:

```text
RETRIEVE
→ VERIFY
→ CONTINUE
```

not:

```text
FAIL TO RETRIEVE
→ REBUILD / INVENT
```

## AG — Chief Facilitator

Immediate mission:

1. Restore/verify tool execution after the reported `googlecloudtools.datacloud_telemetry` PreToolUse interruption.
2. Prove recovery with trivial tool calls before touching project state.
3. Re-verify, do not replay, the already-reported KL-2609-01 and KL-2609-04 receipts.
4. Keep KL-2609-03 on HOLD pending Chief Architect authorization.
5. Keep KL-2609-05 sealed/unlocked.
6. Resume KL-2609-02 edge verification only after tool-plane recovery is proven.
7. Seed the current C.L.E.A.R./CV/job-fire continuity into the local GSMB surfaces without redesigning the CV pack.

Required output:

```text
AG_TOOL_RECOVERY_RECEIPT
+
KL-2609-02 edge verdict
+
local GSMB seed confirmation
```

## Cursor — Lead Developer

Immediate mission:

1. Freeze the CV pack structure as READY; no cosmetic rewrite loop.
2. Preserve the corrected OpenAI Academy C.L.E.A.R. provenance.
3. Preserve `CLEAR_VERIFICATION_AUDIT_2026-09-11.md` and the manifest pointer.
4. Keep the following as execution-open, not document-open:
   - RAY submission
   - prepared drafts not sent
   - WNS application
5. Keep soft/TBC facts explicitly non-authoritative until resolved.
6. Produce a compact execution handoff that the user can act on without re-reading the whole CV estate.

Required output:

```text
JOB_FIRE_EXECUTION_HANDOFF.md
```

No claim of `Complete` until the real actions occur.

## Berea — Independent Witness

Immediate mission:

Audit the **boundary between evidence and propagation**, not the CV wording.

Questions:

```text
What is independently evidenced?
What is Cursor testimony?
What is still TBC?
What action would convert each open state into a receipt?
```

Required output:

```text
BEREA_JOB_FIRE_WITNESS_2026-09-11.md
```

Do not duplicate Cursor's CV edits.

## Minstrel — Vibe Bootstrap

Minstrel is still being established. Do not load it with high-authority mutation work yet.

First mission:

1. Declare runtime identity/provenance.
2. Read the active seed and handoff artifacts.
3. Summarize the current state without modifying it.
4. Demonstrate retrieval of:
   - C.L.E.A.R. canonical expansion;
   - CV pack READY state;
   - three job-fire open actions;
   - soft/TBC boundaries;
   - current AG environment interruption/recovery lane.
5. Leave one bootstrap receipt.

Required state:

```text
MINSTREL_VIBE = OBSERVE → RECONSTRUCT → RECEIPT
```

No architecture authority is granted by onboarding alone.

## Chief Architect sequencing

```text
AG     → restore execution plane + resume KL-2609-02
Cursor → freeze pack + prepare job-fire action handoff
Berea  → independent evidence witness
Minstrel → reconstruct state and prove continuity

USER   → chooses/executes consequential job submissions/sends
```

## Stop conditions

Stop and HOLD if any agent:

- claims to have opened an artifact it did not open;
- converts a retrieval miss into non-existence;
- marks a real-world submission complete without a receipt;
- changes job facts to make a CV look stronger;
- replays a completed infrastructure mutation without checking prior receipts.

## Next CA checkpoint

Return to Forge with only the deltas:

```text
AG recovery + KL-2609-02 result
Cursor job-fire handoff
Berea witness
Minstrel bootstrap receipt
```

No full-state retelling required.
